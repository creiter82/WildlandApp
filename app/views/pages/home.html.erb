<div class="container">
  <div class="jumbotron">
    <h1 class="display-3">Welcome <%= current_user.first_name.capitalize if current_user %></h1>
    <!-- <p class="lead">Find me in app/views/pages/home.html.erb</p></p>
    <hr class="my-4">
    <p>It uses utility classes for typography and spacing to space content out within the larger container.</p>
    <p class="lead">
      <a class="btn btn-primary btn-lg" href="#" role="button">Learn more</a>
    </p> -->
  </div>

  <% if logged_in?(:admin) %>  
      <h3>Users Awaiting Approval</h3>
      <table class="table table-hover d-flex justify-content-start">
        <% @users.each do |user| %>
          <tr>
            <td><%= user.name.titleize %>
            <td><%= link_to "Edit", edit_user_path(user) %>
          </tr>
        <% end %>
      </table>
  <% end %>  

</div>  