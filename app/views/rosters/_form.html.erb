<% if roster.errors.any? %>
  <div id="error_explanation">
  <h2><%= pluralize(roster.errors.count, "error") %> prohibited this roster from being saved:</h2>

    <ul>
    <% roster.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>


  <div class="col-md-6">
    <%= form_for(roster, :html => {class: 'form-horizontal', role: 'form'}) do |f| %>
      <% if roster.name %>
        <div class="form-group row">
          <%= f.label :name, class: 'col-sm-4 control-label' %>
          <div class="col-6">
          <%= f.text_field :name, value: @roster.name.capitalize, class: 'form-control', readonly: true %>
          </div>
        </div>
      <% end %>
      <div class="form-group row">
        <%= f.label :start_date, 'Start Date:', class: 'col-sm-4 control-label' %>
        <div class="col-6">
          <div class="input-group" id="start_date">
            <%= f.text_field :start_date, placeholder: '2018-09-18', class: 'form-control', id: 'start_date' %>
            <span class="input-group-addon">
              <span class><%= fa_icon "calendar" %></span>
            </span>
          </div>  
        </div> 
      </div>

      <div class="form-group row">
        <%= f.submit class: "btn btn-sm btn-outline-primary" %>
      </div>
    <% end %>
  </div>  
  
